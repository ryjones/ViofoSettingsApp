# Local test data — a firmware image and a real exported config. Never commit
# either: the firmware is VIOFO's to distribute, and the config is personal.
FWA329S.bin
viofo_config.ini

.DS_Store

# Live captures from a camera: contain the owner's settings and licence plate.
dump/

# Generated by tools/make-icon.sh
ViofoConfig/AppIcon.icns
